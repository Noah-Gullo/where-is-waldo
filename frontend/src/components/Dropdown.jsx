function Dropdown({x, y}){
    return (
        <>
            <div id="dropdown" style={{
                position: 'absolute',
                left: `${x}px`, 
                top: `${y}px`,
            }}>
                <h1>a</h1>
            </div>
        </>
    )
}

export default Dropdown